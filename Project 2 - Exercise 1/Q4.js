let shiftValue = 3; // Example shift value, could be any number
if (Number.isInteger(shiftValue)) {
  console.log("shiftValue is an integer.");
} else {
  console.log("shiftValue is NOT an integer.");
}
